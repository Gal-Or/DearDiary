<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/image_IMG_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/background_s"
        android:alpha="0.2"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="15dp"
        android:orientation="vertical"
        android:layout_margin="10dp"
        >

        <com.mikhaellopez.circularimageview.CircularImageView
            android:id="@+id/image_IMG_user"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_margin="30dp"
            android:src="@drawable/man"
            app:civ_border_color="#090909"
            app:civ_border_width="4dp"
            app:civ_shadow="true"
            app:civ_shadow_color="#090909"
            app:civ_shadow_radius="0dp"
            />

        <TextView
            android:id="@+id/image_LBL_save"
            android:layout_width="180dp"
            android:layout_height="50dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="5dp"
            android:background="@drawable/bg_btn"
            android:drawableLeft="@drawable/save"
            android:elegantTextHeight="true"
            android:gravity="center"
            android:paddingLeft="15dp"
            android:text="Save Image"
            android:textColor="@color/main_color"
            android:textSize="18dp" />

        <TextView
            android:id="@+id/image_LBL_back"
            android:layout_width="180dp"
            android:layout_height="50dp"
            android:layout_marginTop="5dp"
            android:background="@drawable/bg_btn"
            android:drawableLeft="@drawable/back"
            android:elegantTextHeight="true"
            android:gravity="center"
            android:paddingLeft="15dp"
            android:text="Back   "
            android:textColor="@color/main_color"
            android:textSize="18dp" />




    </LinearLayout>

</RelativeLayout>